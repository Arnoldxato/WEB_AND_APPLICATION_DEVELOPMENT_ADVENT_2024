//logical operators(&&,||,!)
// && logical operator is used to execute code if both conditions are true
// || logical operator (or) is used to execute code if either conditions are true
// ! logical operator is NOT used to execute code 

// person elligible to vote in uganda
Age = 18;
let citizenship = "ugandan";
if ()